# 复制为 compose-cli.env 或运行: ./scripts/write-compose-cli-preference.sh
# 1 = 使用「docker compose」与 compose-v2/compose.yaml
# 0 = 使用「docker-compose」与 docker-compose.yml
# 命令行 --compose-v2 / --compose-v1 优先于本文件。
G2RAIN_USE_COMPOSE_V2=0
