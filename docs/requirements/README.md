# 需求记录

本目录记录需要跨提交追踪的部署需求。每项需求建议使用 `YYYYMMDD-short-name.md`，至少包含：

- 目标环境、服务范围和非目标
- 配置、端口、卷、网络和依赖变化
- 初始化、升级、停止、回滚和清理语义
- 凭据、密钥、数据与供应链安全影响
- 静态验证、运行时验收和灾难恢复标准

只有一个明确标记为 `active` 的需求时，才能由 `develop` 命令默认执行。
