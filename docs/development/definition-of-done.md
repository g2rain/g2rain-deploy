# 完成标准

- 变更目的、目标环境和影响服务明确。
- V1/V2 Compose 与相关业务片段均可解析，Shell 语法通过。
- 服务名、仓库、目录、镜像和构建命令映射一致。
- 新增参数、配置、端口、卷、清理行为和运行流程已同步文档。
- 不提交真实 `.env`、生产凭据、私钥、日志、数据、`codes/` 或安装标记。
- 破坏性操作具有显式参数、清晰提示和可执行的备份/回滚方案。
- 若执行过运行时验证，记录 Docker Context、服务健康、冒烟结果和数据影响；未执行则明确说明。
