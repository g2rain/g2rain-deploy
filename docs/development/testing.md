# 验证策略

## 当前基线

2026-09-06：10 个 Shell/Include 文件通过 Git Bash `bash -n`；Docker Compose V2.35.1 成功解析 V1/V2 主配置和默认 CMS 合并配置。V1 文件产生 `version` 属性已过时警告。未执行容器运行测试。

## 变更验证

- Shell 语法：所有 `.sh` 和被 `source` 的 `.inc`。
- Compose：V1、V2、每个业务片段及常用组合。
- 服务映射：默认项、追加项、相同 Compose 服务覆盖和指定片段顺序。
- 参数：帮助、缺失参数、未知参数、V1/V2 覆盖优先级。
- 生命周期：在隔离 Docker Context 中验证初始化幂等、健康等待、指定服务更新、普通停止和显式清理。
- 数据：验证已有/空数据卷、数据库初始化、备份与回滚。
- 安全：扫描固定凭据、私钥、危险权限和不可信命令执行入口。

仅静态解析通过不能证明部署成功；运行时验证必须说明使用的环境、服务范围和数据影响。
