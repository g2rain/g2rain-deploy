# Git 工作流

- 每个提交聚焦一个部署行为或一个清晰的配置治理事项。
- 修改前检查工作区，保留用户已有配置与变更。
- 不提交 `.env`、运行日志、数据卷内容、`codes/`、安装标记或生成的私钥。
- Compose 服务变化应同时评审 V1、V2、业务片段、服务映射和 Nginx 配置。
- 合并前执行静态验证；涉及真实部署时先在隔离环境演练。
- 版本升级记录镜像 digest、源码提交与迁移/回滚要求，避免只使用不可追溯的 `latest`。
